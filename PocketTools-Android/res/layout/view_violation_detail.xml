<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/color_gradient_bg" >

    <ImageView
        android:id="@id/img_bg"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:scaleType="centerCrop" />

    <com.juhe.pockettools.commonview.TopActiveBarView
        android:id="@id/action_bar"
        android:layout_width="fill_parent"
        android:layout_height="50.0dip" />

    <TextView
        android:id="@id/detail_no_data"
        style="@style/text_20_white"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center"
        android:layout_marginTop="120.0dip"
        android:text="@string/violation_detail_no_data"
        android:visibility="invisible" />

    <ListView
        android:id="@id/list_violation_detail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="50.0dip"
        android:cacheColorHint="#00000000"
        android:clipChildren="true"
        android:clipToPadding="true"
        android:divider="@color/listview_divider_color"
        android:dividerHeight="1.0px"
        android:isScrollContainer="false"
        android:listSelector="@color/transparent"
        android:scrollbarThumbVertical="@drawable/custom_scroll_style" />

</FrameLayout>